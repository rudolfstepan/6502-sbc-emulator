MEMORY {
    ROM: start = $C000, size = $4000, fill = yes, fillval = $EA;
}
SEGMENTS {
    CODE:    load = ROM, type = ro, start = $C000;
    RODATA:  load = ROM, type = ro;
    VECTORS: load = ROM, type = ro, start = $FFFA;
}
